CREATE TABLE [kcsd].[kct_pushsort] (
    [kc_push_sort] VARCHAR (4)  NOT NULL,
    [kc_sort_desc] VARCHAR (30) NOT NULL,
    CONSTRAINT [PK_kct_pushsort] PRIMARY KEY CLUSTERED ([kc_push_sort] ASC)
);

